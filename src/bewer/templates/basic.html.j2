<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{ title | default('Alignment Report', true) }}</title>
    <style>
        body {
            font-family: 'Courier New', Consolas, monospace;
            font-size: 14px;
            background-color: {{ base_color_scheme.BG_COLOR }};
            color: {{ base_color_scheme.TEXT_COLOR }};
            min-width: 1000px;
            max-width: 1000px;
            margin: 0 auto;
        }
        body > div,
        body > table {
            display: block;
            overflow-x: auto;
        }
        h1 {
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
            color: {{ base_color_scheme.DIV_TEXT_COLOR }};
            margin-bottom: 30px;
        }
        .alignment-container {
            border-radius: 10px;
        }
        .alignment-table {
            background-color: {{ base_color_scheme.DIV_BG_COLOR }};
            color: {{ base_color_scheme.DIV_TEXT_COLOR }};
            border-radius: 10px;
            border-collapse: collapse;
            margin-bottom: 15px;
            width: 100%;
        }
        .alignment-table-lines {
            text-align: right;
            padding-left: 10px;
        }
        .alignment-table-names {
            text-align: left;
            font-weight: normal;
            padding: 0px 10px;
        }
        .alignment-table th {
            font-weight: normal;
            text-align: left;
            border-radius: 10px 10px 0 0;
            background-color: rgb(89, 89, 89);
            color: #ffffff;
            padding: 10px 10px;
        }
        .alignment-table tr.alignment-row:nth-child(even) td {   /* odd rows starting from row 3 */
            padding-top: 15px;
            padding-bottom: 5px;
        }
        .alignment-table tr:nth-child(odd) td {   /* odd rows starting from row 3 */
            padding-top: 5px;
            padding-bottom: 15px;
        }
        .legend-container {
            margin-top: 15px;
            margin-bottom: 15px;
            padding: 10px;
            border: 1px solid #acacac;
            border-radius: 10px;
            position: sticky;
            top: 0;
            background-color: #ffffff;
        }
        .legend-container-item {
            display: inline-block;
            margin: 0px 20px;
        }
        .sticky-wrapper {
            overflow: visible;
            /* The legend will stop sticking when this container scrolls out of view */
        }
        .summary {
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
            margin-bottom: 20px;
            padding: 15px;
            background-color: #f7f7f7;
            border-radius: 8px;
        }
        .summary p {
            margin: 5px 0;
            line-height: 1.5;
        }
    </style>
</head>
<body>
    <h1>{{ title | default('Alignment Report', true) }}</h1>

{% set summary_content %}
{% with title="Dataset Summary", summary=summary %}
{% include 'summary_table.html.j2' %}
{% endwith %}
{% endset %}
{{ summary_content | indent_tabs(1) | safe }}

<div class="sticky-wrapper">

{{ alignment_kwargs["color_scheme"].to_html() | safe }}

{% for example in dataset %}
{{ example.metrics.get(alignment_type).ops.to_html(**alignment_kwargs) | safe }}
{% endfor %}

</div>

</body>
</html>
